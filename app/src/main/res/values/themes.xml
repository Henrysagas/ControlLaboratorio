<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ControlLaboratorio" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:buttonStyle">@style/Widget.Material3.Button</item> <!-- Estilo base de botones -->

    </style>

    <!-- Tema para botones-->
    <style name="CustomButtonStyle" parent="Widget.Material3.Button">
        <item name="android:background">@color/primario</item> <!-- Fondo del botón -->
        <item name="android:textColor">@color/white</item> <!-- Texto del botón -->
        <item name="android:radius">10dp</item>
    </style>

</resources>