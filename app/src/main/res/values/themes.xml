<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ControlLaboratorio" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:colorBackground">@color/white</item> <!-- Fondo claro -->
        <item name="android:textColor">@color/black</item> <!-- Texto oscuro -->
        <item name="android:buttonStyle">@style/Widget.Material3.Button</item> <!-- Estilo base de botones -->
    </style>

    <!-- Tema para botones-->
    <style name="CustomButtonStyle" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/primario</item> <!-- Fondo del botón -->
        <item name="android:textColor">@color/white</item> <!-- Texto del botón -->
    </style>
</resources>